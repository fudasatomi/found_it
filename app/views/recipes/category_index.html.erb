<div class="conteiner">
	<div class="row">
		<div class="col-xs-10 col-xs-offset-1">
			<%= render 'category', categories: @category_list %>

			<div class="col-xs-8">
				<h2><strong><%= @category.name %></strong>のレシピ一覧</h2>
					<%= render 'recipes_index', recipes: @recipes %>
			</div>
		</div>
	</div>
</div>
